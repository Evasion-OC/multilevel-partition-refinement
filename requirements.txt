# Runtime dependencies, excluding torch (see hpc/setup_env.sh, which installs a
# CUDA-matched torch from the PyTorch index so the GPU build matches the cluster driver).
# numpy pinned <2.0 for ABI safety with the cluster's Python/3.11.10 + libffi module
# stack); scipy is pinned to the numpy<2-compatible line.
numpy>=1.24,<2.0
scipy>=1.10,<1.13
scikit-learn>=1.3,<1.6   # srmp.initial_partition uses sklearn.cluster.KMeans
