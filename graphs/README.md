# Demo graph

`rdb3200l.graph`: the rdb3200l reaction-diffusion matrix from the SuiteSparse
collection (Bai group), converted to METIS graph format. Shipped so that
`src/demo.py` can partition a real benchmark graph end to end without any
external data. One of the benchmark graphs of Chapter 5.
